CREATE TABLE students (
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        name TEXT NOT NULL,
        email TEXT UNIQUE NOT NULL,
        id_course INTEGER REFERENCES courses(id)
);

DROP TABLE students;

CREATE TABLE courses (
      id INTEGER PRIMARY KEY AUTOINCREMENT,
      name TEXT NOT NULL,
      workload INTEGER NOT NULL
);

DROP TABLE students;

INSERT INTO students (name, workload)VALUES
  ("web Development", 360),
  ("Data Analysis", 360),
  ("Cyber Security", 360),


SELECT * FROM students;
SELECT * FROM courses;
